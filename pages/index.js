// pages/index.js

// 这是一个最基础的 React 组件，没有任何复杂的依赖和逻辑。
// 它保证了页面可以被成功渲染。
export default function HomePage() {
  return (
    <div style={{ padding: '40px', fontFamily: 'sans-serif' }}>
      <h1>Homepage</h1>
      <p>The proxy service is active.</p>
      <p>The build is now fixed.</p>
    </div>
  );
}
